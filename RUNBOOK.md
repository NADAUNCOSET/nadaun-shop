# NADAUN Shop 운영·이전 기준

## 단일 원본

이 프로젝트 폴더가 Mac·Windows 공용 NAS 원본이다. 상위 허브의 `CLAUDE.md`를 함께 따른다. 배포는 `NADAUNCOSET/nadaun-shop` → Vercel `nadaun-shop` → `https://shop.nadaun.co`다.

## 2026-09-07 대표 결정

- 아임웹 `https://rainbowshop.imweb.me/`에서 나다운 샵으로 빠르게 이전한다.
- 자체 스마트스토어는 `https://smartstore.naver.com/rainbowbene`다. 그 카테고리·상품 목록을 이전에 활용한다.
- 아임웹에서는 DJI 브랜드의 상품과 더 상세한 브랜드 분류를 가져온다.
- 화면 구성과 탐색 구조의 최우선 참고는 `https://kppkpp.co.kr/`다. KPP 전체 상품을 수록하고 자체 상품을 합친다.
- 1차 작업은 **브랜드 → 브랜드 안의 원본 세부 카테고리 → 상품**이다. 브랜드마다 전용 페이지로 진입한다. 브랜드 전용 분류명·계층·메뉴 순서를 보존한다.
- 상품 등록이 완료된 뒤 **제품 종류별 통합 탐색**을 추가한다. 삼각대·에어블로어·필터·ND필터 등 한 제품 종류를 선택하면 여러 브랜드의 해당 상품을 함께 보여준다.
- 통합 카테고리는 사람들이 많이 찾는 순서를 지향한다. 실측 검색·조회 자료 없이 임의 순서를 실제 인기순이라고 표시하지 않는다. 운영자의 수동 순서도 보존할 수 있어야 한다.
- KPP에서 상품이 업데이트되면 나다운 샵에도 자동 반영되도록 연동한다. 원본 상품번호로 변경을 추적하고, 자체 상품 및 운영자 수정값을 별도로 보존한다.

이 결정은 과거의 "스몰리그 한 브랜드만 노출" 및 12개 고정 대분류를 먼저 통과하는 화면보다 최신 기준이다. 기존 다른 공급처의 보관 데이터 전체를 공개하라는 뜻은 아니다.

## 데이터 원칙

- 브랜드별 분류와 제품 종류별 분류는 독립 필드로 보존한다. 한 상품이 여러 경로에 포함될 수 있다.
- 원본별 상품번호와 출처 URL을 보존한다. 이름이 비슷하다는 이유만으로 다른 규격·옵션·세트를 합치지 않는다.
- KPP 공개 판매 정보와 자체 스마트스토어 판매 정보를 구분한다. 공급처의 품절 상태를 우리 재고로 단정하지 않는다.
- KPP 가격은 기존 대표 기준인 정가 우선이며, 판매가·할인가도 원본 필드로 구분해 보관한다.
- 수집 실패·일시적 빈 응답을 상품 삭제로 처리하지 않는다. 완전한 목록 대조 후 상태 변경을 판정한다.
- 상품 API의 비공개 관리 필드·인증정보·고객/주문 데이터는 공개 카탈로그에 넣지 않는다.
- 기존 상품 원본·R2 에셋은 임의로 삭제하지 않는다.

## 접근 수단

- 공식 API → 기존 프로젝트 함수/스크립트 → CLI → 브라우저 순서다.
- 스마트스토어·아임웹의 기존 인증 연결은 NAS 허브 `nadaun_order_sync`에 있다. 키를 프로젝트나 Git에 복사하지 않는다. 상품 읽기만 수행한다.
- 스마트스토어 상품 목록은 네이버 커머스 API를 우선한다. 스토어에서 직접 만든 전시 카테고리는 공식 API가 지원하지 않으므로 공개 화면의 메뉴를 보조로 확인한다.
- KPP는 기존 `_scraper/kpp.py`, `_scraper/brand_mirror.py`의 원본 구조 파싱 경험을 재사용한다. 브랜드 메뉴는 `.brand_main_menu`에 한정하고, 목록은 해당 상품 컨테이너에 한정한다. 공통 메뉴·추천 상품·옵션 썸네일을 목록에 섞지 않는다.

## 작업 전 확인한 상태

2026-09-07 점검:

- `shop.nadaun.co` HTTP 200. 정적 HTML 및 Python 빌더 구조다.
- 기존 NAS 데이터에 KPP 상품 레코드 3,240개가 있다. 최신 전체 수집 완료를 의미하지 않는다.
- 스마트스토어 공개 전체상품 화면은 6,964개를 표시했다. API 목록과 판매/노출 상태를 대조해야 한다.
- 작업 시작 시 기존 미커밋 변경은 1,764건이다. 사용자 소유 변경이므로 되돌리거나 새 작업과 함께 일괄 커밋하지 않는다.
- `index.html`, `_scraper/build_shop.py`, `_scraper/build_detail.py`, `_scraper/build_brands.py`, `data/_approved.json`은 작업 시작 시 clean이었다.
- 자동 동기화의 실행 주기·실행 주체·마지막 검증 결과는 실제 연결을 완료한 뒤 이 문서에 기록한다. 스크립트만 만들고 자동 연동 완료로 보고하지 않는다.

## 완료 검증

원본 목록 수·고유 상품번호·카테고리별 소속·가격 필드·이미지·상세 링크를 대조한다. PC·모바일에서 브랜드 진입, 하위 분류, 검색, 페이지 이동을 확인한다. 변경분만 커밋·푸시하고 Vercel Ready 및 라이브 결과를 검증한다. 자동 동기화는 실제 실행과 재조회까지 확인한다.

## 1차 구현과 실행 방법

- 새 원본 수집: `_scraper/sync_shop_sources.py smartstore`, `imweb-dji`, `_scraper/sync_kpp_catalog.py`.
- 상세 수집: `_scraper/enrich_shop_sources.py smartstore|kpp`. 네이버 상세 API는 응답 헤더 기준 초당 2건이므로 캐시의 수정일이 달라진 상품만 재조회한다. KPP는 공개 상세를 주기적으로 재조회한다.
- KPP 기본 정렬은 요청마다 순서가 달라진다. **반드시 `sort=it_time&sortodr=desc`**로 페이지를 순회한다. 수집 결과 620개 카테고리 경로, 1,240페이지, 고유 상품 4,014개. 상세에서 판매 불가/자료 없음으로 2회 확인된 상품은 공개에서 제외한다.
- 스마트스토어는 공개 상품 6,964개, 아임웹 DJI는 149개 중 공개 146개다. 아임웹 `offset`은 행 번호가 아닌 **1부터 시작하는 페이지 번호**다.
- 정적 빌드: `_scraper/build_catalog.py`. `--allow-pending`은 상세 수집 중 로컬 검수에만 쓴다. 최종 자동 발행은 모든 상세 검증을 요구한다.
- 화면: `/` 브랜드 중심 메인, `/brands/<브랜드>.html` 전용몰, `/catalog.html` 목록, `/item.html?id=<원본 상품번호>` 상세. 기존 메인은 `/rental.html`로 보존했다.
- 스몰리그 모델번호와 틸타 SKU는 브랜드·판매/렌탈 구분·규격/색상 접미사까지 포함한 상품번호가 각 출처에서 하나씩 정확하게 일치할 때만 합친다. 애매한 번호·복수 후보는 합치지 않는다. 기존 원본 상품번호 링크는 새 대표 상품으로 연결한다.
- KPP와 직접 대응되지 않는 스마트스토어 상품은 네이버 표준 상품 분류를 유지한다. 스마트스토어 **전시 카테고리와 동일하다고 간주하지 않는다.** KPP 원본 브랜드몰 분류 및 DJI 아임웹 세부 분류가 1차 화면의 기준이다.
- 초기 카탈로그는 외부 구매 링크였으나, 아래 추가 결정으로 자체 장바구니·주문서와 토스페이먼츠 전환을 준비한다. 실결제 활성화에는 상점 계약·키·주문 저장소 검증이 필요하다.

### 운영자 수정값

`data/catalog/overrides.json`의 `products`에 원본 상품 ID별로 `name`, `price`, `sale_price`, `category_ids`, `type_ids`, `hidden`, `status`를 지정할 수 있다. 원본 동기화는 이 파일을 덮어쓰지 않는다. 할인 가격을 수정할 때는 `sale_price`를 함께 지정한다. `brand_order`는 브랜드 순서를 보존한다. 통합 제품 종류 화면은 `category_browsing_enabled: false`로 준비 상태다.

### 자동 실행

`_scraper/shop_sync.py`는 원본 수집 → 상세 변경 수집 → DJI 미리보기 최적화 → 빌드 → 회귀검증 → 생성 파일만 커밋·푸시 → Vercel Ready 및 라이브 revision 재조회까지 실행한다. API 키는 NAS 허브에 남는다. 실패 시 공개 사이트는 마지막 배포를 유지한다. 수집 수가 이전보다 15% 이상 감소하거나 코드/운영자 설정에 미커밋 수정이 있으면 자동 발행을 중단한다.

`_scraper/install_shop_sync.py`로 Mac LaunchAgent `co.nadaun.shop.catalog-sync`를 설치한다. 기본은 **매일 오전 8시**다. 이 Mac의 전원·네트워크와 NAS 마운트가 필요하다. 설치 확인과 실제 실행 결과는 아래 완료 기록에 남긴다. 동작 상태는 `_scraper/.sync-state/`에 저장되며, NAS 파일 잠금으로 중복 실행을 막는다. 네이버 주문·렌탈 자동화에는 손대지 않는다.

### 자동 갱신 추가 검증 기준

- KPP 목록의 이름·가격·판매가·썸네일·공급처 상태가 바뀌면, 상세 캐시 유효 시간 안이어도 해당 상세를 다시 수집한다. 이전 상세 가격으로 최신 목록 가격을 덮어쓰지 않는다.
- 자동 커밋은 생성 파일의 정확한 목록만 지정한다. 같은 폴더에 사용자가 만든 다른 파일, 기존 `data/products/` 원본, `overrides.json`은 자동 커밋 대상이 아니다.
- 구매 상품이 없는 브랜드는 렌탈 탭으로 바로 열린다. 브랜드 카드 상품 수는 기본 진입 목록과 일치한다.
- Mac 로그인 때에도 자동 실행되며, 최근 6시간 안에 검증을 마쳤으면 중복 실행을 생략한다. 같은 Mac에서 종료된 이전 프로세스의 잠금은 기록 후 복구한다.
- macOS launchd의 실행 준비 단계가 SMB 폴더에 접근하지 못해 `EX_CONFIG(78)`로 종료되는 것을 방지하려고, 시작 디렉터리와 표준 출력 로그만 로컬에 둔다. 로그는 `~/Library/Logs/NADAUN/shop/`, 코드·인증·수집 결과·검증 상태의 원본은 계속 NAS다. 실행 상태는 NAS `_scraper/.sync-state/last-success.json`과 LaunchAgent 종료 코드로 확인한다.
- 일부 스마트스토어 원문에 이미 만료된 외부 상세 이미지가 있다. 이미지 한 장의 실패가 상세 영역 전체의 비율·접근성 이름을 바꾸지 않도록 해당 이미지 자리에 안내만 표시한다. 다른 상세 이미지와 구매 링크는 유지한다. 갤러리에서 정상 이미지로 바꾸면 실패 표시를 해제한다.
- 첫 예약 실행에서 상품 수집·상세 검증·빌드는 통과했으나, NAS 대상 `git diff --check`가 180초 제한에 걸렸다. 생성 파일만 스테이징한 뒤 그 스냅샷을 한 번 검사하도록 바꾸고 Git 명령 제한은 900초로 조정했다. 스테이징 전·후 모두 다른 작업 파일의 혼입을 차단한다. 완료한 수집 결과는 다시 수집할 필요 없이 `shop_sync.publish_existing()` 내부 함수로 배포를 재개할 수 있다. 단, 같은 데이터의 엄격한 빌드와 회귀검증이 이미 성공했고 실행 잠금이 해제됐는지 먼저 확인한다.
- LaunchAgent는 `ProcessType=Standard`를 사용한다. macOS 기본 문서(`man launchd.plist`)상 `Background`는 추가 자원 제한이 적용되므로, NAS의 많은 작은 파일을 검사하는 이 작업에 불필요한 I/O 지연을 더하지 않게 한다. 소스 수집은 계속 3개 요청 이내로 제한한다.

### 2026-09-07 공개 및 연동 검증

- 공개 카탈로그: 137개 브랜드, 판매 10,221개·렌탈 220개. 스마트스토어 6,964개, KPP 4,014개, 아임웹 DJI 146개의 상세 검증 완료. 정확히 일치하는 모델 677건 통합, KPP 판매 불가 6건은 공개 목록에서 제외했다.
- PC·모바일 브랜드 탐색, DJI/SmallRig 하위 분류, 검색, 가격 정렬, 페이지 이동, 옵션, 기존 스토어 구매 링크와 구형 KPP 상품 주소 리디렉션을 확인했다. 회귀검증 9건 및 JavaScript 구문 검사를 통과했다. 원본/API 상세 캐시와 운영 설정의 공개 접근은 404다.
- 예약 실행으로 전체 원본을 다시 수집하고 KPP 상세 변경 1건을 갱신했다. 위 Git 시간 제한 문제를 수정한 뒤 같은 검증 결과의 배포를 재개해 **15:53 KST** Vercel Ready와 라이브 revision `ec552a6550ae677e` 일치를 확인했다. 데이터 커밋 `a1da40a8`, 배포 `dpl_ChNDuojWN6i3sATDZc53en1zCDX6`.
- 다음 작업에서는 이 초기 기록보다 NAS `_scraper/.sync-state/last-success.json`의 최신 시각·커밋·배포·revision을 우선한다. 실패 기록은 복구 후에도 이력으로 남으며, 성공 기록보다 오래된 실패를 현재 장애로 판단하지 않는다.
- 스마트스토어의 기존 전시 대분류 19개는 `data/catalog/reference-smartstore-menus.json`에 참조용으로 보존했다. 그 하위 전시 분류 전체를 API로 복제한 결과는 아니다.

기존 `/product/<브랜드>/kpp-<상품번호>.html` 주소는 최신 상세 화면으로 연결한다. 기존 상세 파일은 NAS에 그대로 보존한다.

## 2026-09-07 저녁 추가 확정 — 통합 쇼핑몰

- 미디어박스 `https://mediaboxstore.com/`의 넉넉한 이미지·여백을 참고하고, 나다운의 타이포·옐로우 포인트로 정제한다. 왼쪽 구매/렌탈 선택과 브랜드→카테고리 구조를 유지한다.
- 모든 브랜드를 대상으로 중복을 검증한다. 원본을 보존하고 이름/브랜드/SKU/옵션/색/수량/상태로 검증한 동일 상품만 공개에서 통합한다. `dedup-rules.json`은 검토한 예외, `dedup-audit.json`은 재생성 가능한 비공개 근거다.
- 푸터는 `rental.html`의 기존 승인 내용을 유지한다. 이용약관·개인정보처리방침·배송 및 교환/반품 안내는 실제 페이지로 연결한다. 통신판매 신고번호가 원문 상단/하단에 다르게 표기된 것은 대표 확인 대상으로 기록한다.
- 오른쪽 하단 네이버 톡톡·카카오 상담을 모든 화면에 고정한다. 네이버 커머스 API 채널 조회로 `w4w1o8` 확인, 카카오는 기존 `_pyNxnxb` 채널이다. 온라인 응답 여부를 실시간으로 조회하는 API는 없으므로 24시간 실시간 응답을 보장하는 문구는 쓰지 않는다.
- 외부 스토어 구매 버튼을 자체 장바구니/주문서 흐름으로 전환한다. 토스페이먼츠를 연결할 예정이며, 현재 Vercel 상점 환경변수는 없다. 키/계약/주문 저장소가 준비되기 전 결제를 받거나 결제 성공을 가장하지 않는다.
- 배송·교환·반품은 자체 스마트스토어의 상품별 조건을 우선한다. 한 상품 배송비를 전 상품에 일괄 적용하지 않는다. 상세가 불일치하는 부분과 사업자 세부 정보는 대표가 검토한다.
- SEO는 80자 이내 설명, 고유 canonical/title/OG, 구조화 데이터, 실제 상품 본문 및 sitemap으로 검사한다. 검색엔진 소유확인·색인 완료·PG 심사 통과는 외부 검증 전 보장하지 않는다.
- 현재 브라우저 런타임에 연결된 브라우저가 0개여서 이 작업의 새 화면 검수는 연결 복구 전 완료로 기록하지 않는다. 원본 아임웹 약관 공개 URL도 접근 제한이므로 그대로 복사했다고 보고하지 않는다.

### 추가 통합 검증 및 후속 검토

- 자체 장바구니는 상품번호/옵션/수량만 브라우저에 보관하고, 이름·배송지·연락처 수집이나 실제 결제·주문 접수는 아직 하지 않는다. `/cart.html`, `/checkout.html`은 noindex다.
- `/item.html?id=...`은 Vercel `/api/product.js`가 최초 HTML부터 고유 상품 제목·설명(80자 이내)·사진·본문·Product 구조화 데이터를 제공한다. 검증된 중복 ID는 대표 상품으로 308, 없는 상품은 404다. 조작한 별점/후기/구매 가능 상태는 넣지 않았다.
- 메인·브랜드·약관 페이지에도 정적 본문을 제공한다. 전체 공개 상품을 `catalog-sitemap.xml`에 수록한다. 자바스크립트는 검색/필터/옵션/장바구니를 보강한다.
- 기존 푸터·상담 정보 충돌, 원문 규격 충돌, 약관 초안, 결제·검색엔진 소유확인·브라우저 검수의 남은 조건은 `REVIEW-2026-09-07.md`에서 이어받는다.
- 이 추가 빌드의 상품 10,074개, 중복 통합 1,042개. 회귀검증 Python 14건 + 상품 응답 Node 4건 통과(전체 상품 10,074개 SEO 메타 포함). 원본 옵션과 SKU의 접미사를 비교해 피지테크 백팩 `P-CB-180`과 교체용 휠 `P-CB-180-01`을 분리했다.

- 이름은 같고 옵션 구성이 다른 16개 상품군은 목록에서 한 카드로 표시한다. 상세의 `옵션 구성 비교`에서 기존 각 구성을 선택할 수 있으며 상품번호·원본 옵션·가격은 합치거나 지우지 않는다. 공개 상품 레코드 10,074개 / 목록 카드 10,058개로 구분하고, 목록 카드의 동일 브랜드·이름 중복 0건을 검사한다.

- 라이브 검증에서 Vercel의 일반 rewrite보다 기존 `item.html` 정적 파일이 우선되어 검색용 HTML이 누락되는 것을 발견했다. `vercel.json`의 명시적 상품 경로를 filesystem 처리 이전에 배치한다. `/api/product`만 검사하지 말고 반드시 고객이 쓰는 `/item.html?id=...`의 원시 HTML에 실제 상품 h1과 고유 메타가 있는지 확인한다.

## 2026-09-07 19시 후속 피드백

- 마지막 하위 분류에서 상위 이동이 사라진 원인은 상위 이동 링크를 `children.length` 조건 안에 넣었기 때문이다. 하위 분류가 없어도 현재 분류가 있으면 상위 링크를 항상 표시한다. 상단에는 브랜드부터 현재 분류까지 클릭 가능한 경로를 보여준다.
- 주 메뉴와 왼쪽 카테고리의 첫 항목에 골드 톤 `할인상품` 코너를 둔다. `sale=1`로 자체 스토어의 실제 공개 판매가가 정상가보다 낮은 구매 상품만 필터링한다. 공급처 할인 가격을 나다운 할인 가격으로 간주하지 않는다.
- 주문서 상담 버튼 위와 렌탈 상품 상세에 렌탈 안내(장비/일정 전달 → 재고/요금 확인 → 예약 확정/픽업/반납)를 추가한다. 아직 정하지 않은 보증금·신분증·취소 수수료를 임의로 만들지 않는다.

### 검색 및 AI 검색 범위 확장

- 대표 요청: 구매·장비 렌탈·협찬·사진/영상 촬영까지 검색으로 연결하되, 상품 구매/렌탈 화면에 Collective 홍보를 끼워 넣지 않는다.
- `services.html`은 이용·협업 안내 페이지이며 푸터에서 연결된다. 실제 방문객에게 구매/렌탈 절차, 협찬 제안에 필요한 정보, 촬영 제작 문의와 Collective의 역할을 설명한다. 같은 내용을 Organization/Service 구조화 데이터로 연결하며 서로 다른 서비스를 `sameAs`로 처리하지 않는다. Collective 실제 주소 `https://collective.nadaun.co/`의 200 응답과 canonical을 확인했다.
- 상품별 검색어는 원본 상품명과 원본 하위 분류에서 생성한다. 최대 7개 관련 태그를 화면에 표시하며 ND필터/엔디필터, 에어블로우/에어블로워 등 동의어로 검색할 수 있다. 제품 구매와 렌탈 설명을 구분한다. 모든 상품에 협찬/제작 키워드를 넣거나 숨김 문구를 쌓지 않는다.
- 상품 10,074개마다 실제 대표 이미지 URL을 이미지 사이트맵에 연결한다. 현재 수집 데이터에 검증된 재생 영상 메타가 없으므로 VideoObject는 만들지 않는다.
- 검색량·검색 순위 데이터가 없는 상태에서 '인기 검색어' 또는 최상위 노출을 확정하지 않는다. Google/네이버 소유확인 후 실제 검색어 실적에 따라 조정한다. Collective의 소유확인 토큰을 다른 도메인인 샵에 복사하지 않는다.
- 검증: Python 14건 + Node 12건(전체 공개 상품의 설명/태그/이미지 사이트맵, 모든 마지막 하위 분류 상위 이동, 실제 할인 50개, 상품 최초 응답 포함). 배포 후 HEAD의 READY와 실제 상품·서비스 페이지를 API/HTTP로 재검증한다.

## 2026-09-07 기프트·협력사·아트숍 추가 결정

- `https://www.l-mount.co.kr/main/index.php`는 협력사 전체 상품 수록 대상으로 추가한다. `_scraper/sync_partner_catalogs.py`의 공개 읽기 수집으로 원본 57개 분류·335개 상품을 각각의 표시 수량과 대조했다. 브랜드는 `LDL-MOUNT` / `ldl-mount`, 상품 키는 `ldl-원본번호`다. 하위 분류, 상세 원문, 700px 갤러리, 16개 상품의 색상/추가금 옵션을 보존한다. 공급처 공개 가격은 자체 할인으로 표시하지 않고 재고·납기는 상담 확인으로 둔다.
- 나다운이 운영하는 `https://www.nadaun-gift.com/`은 `gifts.html`의 **별도 기프트·굿즈관**으로 연결한다. 원본 390개 카테고리와 홈페이지 전시 상품을 읽고, 12개를 기프트관에 전시한다. 각 링크는 원래 기프트샵 상품/분류의 새 창으로 연결한다. 수량·인쇄·포장 가격이 달라지는 주문은 기프트샵에서 확인한다. 모든 기프트 상품을 나다운샵 장바구니에 복제하거나 재고·주문을 양방향 동기화한 상태는 아니다.
- 할인 코너명은 `할인상품 · 프로모션`. 자체 아임웹의 실제 `할인 프로모션 모음` 37개를 수집하고 `전체 혜택 / 할인상품 / 프로모션` 탭으로 나눈다. 프로모션 소속은 중복 통합과 옵션 묶음 후에도 보존한다. 실제 할인 52개와 프로모션은 서로 다른 조건이다.
- 138개 브랜드 모두 이미지와 이름을 표시한다. 검토한 로고 100개는 `assets/shop/brands`에 로컬 보관하고 출처는 비공개 `brand-assets.json`에 남긴다. 로고 확인이 안 된 38개는 해당 브랜드의 대표 상품 사진을 사용한다. 임의로 다른 회사 로고를 붙이지 않는다.
- 대표 미감 방향은 **독보적인 아트숍**. 메인은 여백·큰 한글 타이포·실제 제품 전시·브랜드 이미지 인덱스·골드 프로모션으로 구성한다. NADAUN 원래 워드마크, 구매/렌탈/기프트 동선, 기존 푸터를 보존한다. GSAP 3.13.0/ScrollTrigger를 로컬 호스팅하며 PC 포인터·모션 허용 환경에서만 장식/전시 모션과 커서를 켠다. 기본 본문은 처음부터 표시하고 모션이나 API를 기다려 숨기지 않는다.
- 메인은 전체 10MB 상품 파일 대신 약 66KB `data/catalog/brands.json`만 추가 로드한다. 기프트관은 카탈로그 API 없이 원문 링크와 카테고리 검색이 동작한다. 메인의 초기 HTML은 `shop_templates/home.html` 한 곳이 원본이고 JS는 브랜드 펼침/검색만 보강한다.
- 원본 상세 이미지 감사에서 2,866개 고유 주소 중 14개 오류를 확인했다. 5개는 오타를 수정한 정상 파일의 실제 이미지까지 확인했다. 나머지 9개(빈 이미지·배너·누락된 GIF/상세 사진)는 공개에서 제외하고 원본/근거를 보존한다. 상세 정보 일부가 없는 3개 상품은 상담 확인 문구를 한 번만 표시한다. 비공개 `partner-image-rules.json`은 정확한 상품번호/원래 URL에만 적용하며 자동 갱신 시에도 보존한다.
- 일일 자동 동기화에 엘디엘마운트 전체, 아임웹 프로모션, 기프트관 분류/전시를 포함한다. 카테고리 수 15% 급감, 원본 총수 불일치, 상세 수집 실패는 발행을 중단한다. 네이버 상품·가격·재고 양방향 쓰기는 아직 켜지 않았다. 공식 커머스 API 연동 시 기준 원장·충돌 처리 결정이 필요하다. 검색 색인은 서치어드바이저의 별도 소유확인/수집 절차다.
- 회귀 검사 Python 16건 + Node 13건, 전체 상품 고유 메타/태그/이미지 사이트맵, 이미지 수정/제외와 기프트 분류 수를 검사한다. 실제 브라우저가 런타임에 연결되지 않아 새 PC·모바일 화면 검수는 남아 있다. 배포 상태와 최종 증거는 `_scraper/.sync-state/release-review.json` 및 `last-success.json`을 우선한다.

## 2026-09-07 전체 폭·옐로우 UI·제품 브랜드 카드

- 대표가 추가한 모바일 레퍼런스 2장과 `https://www.danilodemarco.com/`을 바탕으로 큰 제품 사진, 굵은 제목, 둥근 카드, NADAUN 옐로우 버튼/전시 면을 적용한다. 금색 할인 코너는 유지한다. 전체화면 요청은 브라우저 폭을 쓰는 반응형 레이아웃으로 반영하며, 브라우저 전체화면 권한을 요구하는 버튼은 추가하지 않는다.
- 공통 `.wrap`의 1440px 제한을 해제한다. 좌우 여백은 18px부터 최대 72px 및 기기 안전영역을 적용한다. 큰 화면의 상품/브랜드 열 수는 카드 최소 폭에 따라 자동 증가한다. 모바일은 상품 2열, 브랜드 2~3열, 메뉴 가로 스크롤, 주문서 1열이다. 약관 본문과 상세 설명은 읽기 좋은 폭을 유지한다.
- 본문 16~18px, 상품명 PC 18~20px/모바일 16px, 가격 23~25px, 상세·주문 상품 제목 24px 이상으로 확대한다. 검색/옵션/수량 입력은 모바일에서도 16px 이상, 주요 버튼은 56px 이상이다.
- 주문서는 큰 실제 상품 사진 + 상품/옵션/수량 + 옐로우 금액 카드 + 렌탈 안내 + 구매 상담 카드로 구성한다. 확인이 필요한 상품은 합계에서 제외하고 소계로 명시하며, 확인된 가격이 하나도 없으면 0원 대신 금액 확인 필요를 표시한다. 상담으로 이동하는 버튼이며 아직 결제·주문 접수를 받지 않는다.
- 브랜드 카드 138개는 전부 해당 브랜드에 등록된 대표 상품 사진과 브랜드명으로 표시한다. 로고 자산은 원본/브랜드 제목용으로 유지한다. `_scraper/brand_products.py`에 검토한 브랜드별 모델 선택 기준을 두며 `overrides.json.brand_featured_products`에 브랜드→상품번호로 고정할 수 있다. 가급적 상세 갤러리의 큰 사진을 사용하고 상품 식별자/출처를 보존한다. 소프트웨어는 해당 판매 상품의 원본 표지를 사용한다.
- 섹션과 카드가 화면에 들어올 때 순차적으로 짧게 올라오며, PC 메인 제품/배경은 스크롤 진행도에 반응한다. GSAP/ScrollTrigger는 로컬 파일이다. 기본 내용은 항상 보이고, 모션 감소 설정에서는 모션을 끈다. 브랜드 펼침/검색 및 장바구니 수정 이후에도 새 카드에 적용하며 제거된 요소의 트리거는 해제한다.
- 회사소개 `/about.html`을 추가하고 푸터에 회사소개 / 이용약관 / 개인정보처리방침 / 이용안내 / 배송 및 교환·반품 안내 5개를 연결한다. 이용안내는 기존 `/services.html`을 보강하며 회원 가입 여부, 장바구니 보관, 구매·렌탈·기프트 주문, 변경·취소·배송조회 문의 방법을 설명한다. 회사 정보는 기존 승인 정보만 사용한다.
- 약관·개인정보처리방침은 여전히 원문 대조와 실제 결제/개인정보 처리 흐름 확정이 필요한 초안이다. 기존 푸터의 통신판매 신고번호 충돌도 대표 검토 대상으로 남긴다. 관련 서비스가 검증되기 전 심사 완료나 실결제 가능으로 보고하지 않는다.
- 회귀 검사는 Python 16건 + Node 19건(장바구니/주문서 5건 및 회사/안내 탐색 추가), CSS/JS 구문과 브랜드 사진의 실제 HTTP/이미지 응답을 확인한다. 브라우저 런타임 연결은 여전히 0개이므로 실제 화면·모션 시각 검수는 미완료로 기록한다.

### 21시 브랜드 카드 색상 피드백

- 대표가 브랜드 카드의 연노랑 색감을 촌스럽다고 지적했다. 번갈아 칠하던 연노랑·베이지 배경을 없애고 모든 브랜드 사진 영역을 흰색과 얇은 뉴트럴 테두리로 통일한다. 사진에 `mix-blend-mode:multiply`를 적용하면 색상 배경이 제품까지 물들이므로 브랜드·상품·장바구니·메인 사진은 `normal`로 표시한다.
- 사이트 바탕과 큰 전시·주문·안내 영역은 뉴트럴 톤으로 정리한다. 골드는 작은 주요 동작과 할인 표시에 집중하고, 프로모션 패널은 차콜과 골드 텍스트로 구분한다. 큰 글씨, 사진 크기, 반응형 열 수, 순차 모션은 유지한다. 원본 이미지 파일은 변경하지 않는다.


### 21시 후속 — 배너·스튜디오·주문 구축 순서

- 메인 비주얼은 기존 공용 배너 원본 PC 1920×400 / 모바일 750×600 비율로 표시한다. `data/catalog/banners.json`에서 PC/모바일 한 쌍과 상품 연결을 관리한다. 현재 Manfrotto·Shure·Jackery·Genelec 4개를 재사용한다. 할인 유효성을 다시 확인하지 않은 테더툴스 15% 배너는 새 메인에 게시하지 않는다. 실제 스마트스토어 관리자 업로드나 자동 배너 동기화를 완료한 것은 아니다.
- 배너는 7초 간격으로 전환하고 수동 이전/다음·일시정지를 제공한다. 키보드 포커스·수동 조작 시 멈추고, 숨겨진 탭과 모션 감소 설정에서는 자동 진행을 멈춘다. 배너 안의 글씨·이미지를 자르거나 늘리지 않는다. `나의 장비 찾기` 버튼은 제거하고 헤더 검색을 유지한다.
- 카드의 순차 이동을 강화하고 섹션 제목·프로모션 도형에 스크롤 진행도 모션을 적용한다. 실제 스크롤이나 검색 동작을 가로채지 않는다.
- 상단 스튜디오 안내는 `/studio.html`로 연결한다. 스마트스토어 스튜디오 원본의 공간 사진 9개를 `assets/shop/studio/`에 보존했다. 내용은 자연광·호리존·전동 배경지·시설·최소 2시간 예약 안내만 표시한다. 스튜디오 주소는 2층, 전용 전화는 0507-1313-6232다. 원래 쇼핑몰 푸터의 1층 주소·전화는 그대로다.
- 대표가 주문 관리 창과 토스페이먼츠 구축 순서를 질문하고 아임웹 기본 설정 스크린샷 3장을 제공했다. 구현 범위와 검증 기준은 `COMMERCE-PLAN.md`에 기록했다. 관리자/주문 DB/토스는 설계 단계이며 실결제 가능으로 보고하지 않는다.
- PLTHINK 전체 브랜드 상품 동기화를 추가 요청했다. 과거 `_scraper/makeshop.py`와 현재 공개 목록을 확인했다. 전체 수집·중복 검증·일일 워커 편입 전 연결 완료로 보고하지 않는다. 기존 렌탈 분류 보강, 공급처 품절 변경 반영 주기 단축, 아임웹 이미지 보존도 후속 작업으로 유지한다.


### PLTHINK 접근 제한 — 22시 확인

- 현재 `.brand-category`에서 141개 브랜드 분류를 확인했다. 기존 `makeshop.py`의 전체 `shopdetail` 링크 수집과 `.ck-content` 상세 선택자는 현재 페이지와 다르므로 그대로 실행하지 않았다. 새 읽기 수집기는 목록의 `#productClass .item-list-2021 > .item-list`, 표시 Total, 페이지 번호, 상세 Product JSON-LD·`.thumb`·`.detail-con-img`를 대조한다.
- 3개 작업자의 목록 수집 중 사이트가 HTTP 302 본문으로 “초단위로 페이지를 너무 많이 요청” 보호 메시지를 반환했다. 전체 원본 스냅샷은 생성하지 않았고 공개 카탈로그와 예약 동기화에도 편입하지 않았다. **PLTHINK 전체 동기화는 아직 미완료**다.
- `_scraper/sync_plthink_catalog.py`에 전역 요청 직렬화·요청 종료 후 최소 2.1초 간격, 차단 즉시 후속 요청 중지, 최소 1시간/서버 Retry-After 중 긴 값의 대기 가드를 추가했다. 차단·대기 기록은 비공개 `.sync-state/plthink-rate-limit.json`에 보존한다. 다른 IP·계정·브라우저로 제한을 우회하지 않는다.
- 다음 실행은 대기 시간이 지났고 접근이 정상화된 뒤 이 수집기로 재개한다. 협력사용 공식 API/상품 피드가 제공되면 그 경로를 우선한다. 전체 목록 수·고유 ID·상세·옵션 검증 후 `build_catalog.py` 어댑터, 브랜드 별칭, 중복 감사, `shop_sync.py` 자동 실행을 연결해야 한다. 호출 간격만 낮춘 상태를 동기화 완료로 보고하지 않는다.
- 파서·목록 누락 방지·품절/미확정 옵션·보호 응답 후 추가 요청 0건 검사를 4건 통과했다. 라이브 디자인 배포는 `d8f11898`, revision `444eb82545a8c4bd`로 검증했다. 쇼핑 UI 39건과 별개인 수집기 검사다.


### 23시 후속 — 제품 이미지와 연속 스크롤 모션

- 대표 요청에 따라 세 진입 카드 이름을 **제품 구매 / 제품 렌탈 / 기프트 구매**로 명확하게 바꿨다. 실제 DJI 액션캠, 소니 FX3와 렌즈, 나다운기프트의 필름 키링 사진을 사용한다. PC는 큰 사진 3열과 높이 차이, 모바일은 사진과 이름을 함께 보여주는 가로형 카드 3단이다.
- 구매 원본 `assets/shop/departments/purchase.jpg`는 아임웹 원본(5000×5000, 약 8.6MB)을 보존한 것이다. 실제 화면에서는 이미 보관한 850px `thumbnails/imweb-13283.webp`(약 42KB)를 사용한다. 원본은 배포에서 제외한다. 렌탈 원본은 스마트스토어 `smartstore-12138568438`, 기프트는 자체 기프트샵 `2614403`의 원본 사진이다. 이미지 색상은 바꾸지 않는다.
- 원본 주소: 구매 `https://cdn.imweb.me/upload/S2020100856a601720ff3b/a8a93bcfbc185.jpg`, 렌탈 `https://shop-phinf.pstatic.net/20250719_229/1752933897019D5qOK_JPEG/29126571166279184_1169243570.jpg`, 기프트 `https://cloudfront.imggift.com/shop_images/ra/a/01/7124536_l.jpg`.
- `assets/shop/motion.js`에서 GSAP ScrollTrigger의 진행도를 사진 레이어·제목 줄·텍스트 스트립에 연결한다. 한 번 재생하고 끝내지 않으며 위로 스크롤하면 되돌아간다. 이미지 레이어가 이동하는 동안 카드의 이름·링크 위치는 유지한다. 사진 안쪽 hover와 바깥쪽 scroll 변환을 분리해 서로 덮어쓰지 않게 한다.
- PC의 프로모션/기프트 2개 패널은 기본 세로 흐름 안에서 CSS sticky로 겹쳐 지나간다. 모바일·작은 높이·모션 감소 환경에서는 일반 세로 카드다. 키보드 포커스가 들어오면 겹침을 풀어 두 링크 모두 보이게 한다. 스크롤 입력이나 관성은 가로채지 않는다.
- 프로모션 대표 사진은 빌드 시 실제 자체 할인/프로모션 상품 중 판매 중인 상품에서 선택한다. 해당 제품이 혜택 목록에서 빠지면 다음 유효 상품으로 바뀐다. 가격·할인율을 이미지에 새로 합성하지 않는다.
- 브랜드 펼침/검색과 장바구니 갱신 이후 새 요소를 등록하고 제거된 요소의 모션을 해제한다. 모션 감소 설정 변경 시 wrapper/inline transform/observer/커서를 정리한다. 기본 HTML과 CSS만으로도 내용과 링크가 보인다.
- 스마트스토어 **전체 최신 배너** 요청은 별도 진행 중이다. 공개 홈은 HTTP 429, 연결 가능한 브라우저는 0개였고 현재 배너 원본 폴더를 대표에게 확인 중이다. 현재 공개 4개를 전체 최신 스마트스토어 배너라고 보고하지 않는다. 유효성이 확인되지 않은 테더툴스 15% 배너를 임의로 게시하지 않는다.
- 실제 PC·모바일 브라우저 화면/스크롤 검수는 브라우저 연결 복구 전 완료로 기록하지 않는다. 코드·카탈로그 회귀와 배포 후 HTTP 검증 결과는 비공개 release-review를 기준으로 한다.

- 이번 변경 검증: 카탈로그 Python 16건 + Node 28건(모션 수명주기 5건 포함) 통과. 모션 감소 시 원본 DOM 보존, 검색 결과 교체 시 이전 트리거 해제, 반복 갱신 시 중복 등록 방지, 설정 전환 시 커서·이벤트·타이머 정리, JS 없이도 3개 실제 이미지/링크 표시를 검사했다. CSS 선언 구문 검사도 통과했다.


### 23:33 대표 화면 피드백 — 영문 띠 제거·카드 정렬

- 큰 `SHOP ↗ RENTAL ↗ GIFT` 영문 띠가 이상하다는 피드백에 따라 HTML·전용 스타일·스크롤 트리거를 제거했다. 이런 장식용 대형 반복 문구는 다시 넣지 않는다.
- 구매/렌탈/기프트 카드의 높이를 다르게 보이게 하던 두 번째 46px·세 번째 18px 상단 여백을 제거했다. 세 카드가 같은 행에서 늘어나도록 정렬해 위·아래선과 사진·제목을 나란히 맞춘다. 상품 사진 내부의 스크롤·호버 모션은 유지한다.
- 영문 띠 자리는 남기지 않고 브랜드 섹션 앞의 간격만 반응형으로 조절한다.


### 23:35 브랜드별 보기 — 전체 브랜드 새 창

- 대표 요청: `브랜드별 보기`는 메인 섹션으로 스크롤하거나 목록을 한 번 더 펼치지 않고, 전체 브랜드를 한 번에 보여주는 별도 페이지를 새 창으로 연다.
- `/brands.html`에 현재 전체 138개 브랜드의 대표 상품 이미지·이름·상품 수를 초기 HTML부터 표시한다. 상단 브랜드명 검색은 영문/한글 별칭과 띄어쓰기 차이를 처리한다. 각 브랜드몰도 기존처럼 새 창으로 열린다.
- 공통 상단 메뉴와 메인의 전체 브랜드 링크는 `target="_blank" rel="noopener"`로 이 페이지에 연결한다. 메인의 18개 대표 브랜드는 유지한다. 검색 결과에서 숨긴 사진의 모션은 해제하고 다시 표시할 때 등록한다.
- 메인과 전체 브랜드 페이지는 전체 카탈로그 API를 기다리지 않는다. 새 페이지를 생성 목록·자동 동기화 관리 대상·사이트맵에 포함한다.


### 2026-09-08 제품 우선 탐색 — KPP 구매 분류·SLR렌트 렌탈 분류

- 대표의 최신 요청은 큰 전시 블록보다 빠른 상품 선택이다. 메인의 대형 프로모션/기프트 패널을 제거하고, 구매·렌탈·기프트 진입 카드는 같은 높이의 작은 이미지 링크로 축소한다. 추천 상품을 브랜드 섹션 앞에 놓고 제목도 축소한다. 큰 영문 띠는 다시 넣지 않는다. 제품 사진의 짧은 스크롤·호버 모션은 유지한다.
- 구매 상품은 KPP의 제품별 원본 분류 75개를 보존하고, 카메라·마이크·전원·디스플레이·모니터암 등 실제 보유 상품의 세부 종류를 보강한 **236개 제품 분류**로 탐색한다. 원본 브랜드 카테고리와 상품번호는 바꾸지 않는다.
- 렌탈은 SLR렌트 공개 메뉴 `https://www.slrrent.com/kr/index.php`를 2026-09-07 확인한 구조로 정리한다. `_scraper/references/slrrent-categories.json`에 원본 334개 메뉴와 출처를 보존한다. 브랜드명인 하위 메뉴는 브랜드 필터에서 담당하고, 제품 종류인 95개 메뉴와 자체 중형 카메라·중형 렌즈·스튜디오 3개를 합쳐 **9개 대분류 / 98개 분류**로 표시한다. 경쟁사의 상품·가격·재고를 가져온 것이 아니다.
- 현재 자체 렌탈 **220개**를 시네마, 카메라, 렌즈, 조명, 마이크·오디오, TV·디스플레이, 삼각대·서포트, 촬영 액세서리, 기타 장비에 배치했다. TV처럼 보유 상품이 없는 분류는 0개로 표시한다. 카메라+렌즈/케이지 패키지를 액세서리로 오분류하지 않으며, V마운트 배터리와 렌즈의 PL/E/T 마운트 표기도 구분한다. 시네새들·달리·애플박스·중형 시스템 등 개별 분류를 검토했다.
- **상단 `type` = 브랜드 공통 제품 종류**, **왼쪽 `cat` = 선택한 브랜드의 카테고리**다. 두 조건이 동시에 적용되며 각 분류의 수는 반대쪽 선택을 반영한다. 브랜드를 바꿔도 구매/렌탈, 제품 종류, 검색, 정렬, 품절 제외를 유지하고 이전 브랜드 경로와 페이지 번호만 지운다. 마지막 하위 분류에서도 상위 분류로 돌아갈 수 있다. 전체 펼침에는 모든 깊이의 제품 분류를 표시한다.
- 렌탈 조회는 전체 카탈로그 약 11MB 대신 `data/catalog/rental.json` 약 656KB를 사용한다. 220개 제품의 가격·상태·이미지·경로는 전체 카탈로그와 정확히 같은 데이터다. 빌드와 예약 동기화의 생성/배포 관리 대상에 포함했다.
- `_scraper/product_taxonomy.py`, `_scraper/rental_taxonomy.py`가 빌드마다 분류를 재생성한다. 명시적 KPP/협력사 분류, 제품명/모델, 네이버 원본 분류 순으로 판단하며, 불명확한 구매 상품 441개는 기타 촬영용품에 두고 `_scraper/.sync-state/product-taxonomy-review.json`에 검토 대상으로 기록한다. 운영자가 확인한 분류는 `overrides.json.product_type_overrides`에 고정할 수 있다. 새로운 제품명까지 정확성을 보장하는 무검수 자동 분류로 보고하지 않는다.
- Python 24건, Node 34건, CSS/JS 구문 검사를 통과했다. 자체 렌탈 부분집합, 전체 분류의 상위 경로, 브랜드 격리, 옵션별 중복 카드 방지, 교차 필터 수, 렌즈/배터리/카메라 세트 오분류, 마지막 단계 복귀를 검사한다. 이전 배포와 비교해 **10,412개 제품의 상품번호·가격·재고·옵션 카드 식별자는 변경되지 않았다**.
- 토스 실결제/주문 관리 구축은 여전히 별도 미완료다. 이번 배포의 범위는 상품 탐색과 분류이며 결제 개통으로 보고하지 않는다. 브라우저 런타임 연결이 없어 새 PC·모바일 실제 화면 검수는 남아 있다. 배포 API READY 및 실제 HTTP/데이터 검증 결과는 `.sync-state/release-review.json`과 `last-success.json`에 기록한다.

### 2026-09-08 기프트 전체 미러링·상품별 비공개 공급처

- 대표 요청은 기존 기프트관의 분류/대표 상품 링크를 넘어, 자체 기프트몰 전체 상품·옵션·수량별 가격·품절 변경을 단방향으로 반영하고 나다운 샵에서 결제하는 것이다. 기존 `collect_gift()`의 `complete: true`는 분류/홈 전시 연결에 대한 것으로 **전체 상품 수집 완료를 의미하지 않는다**.
- 대표 계정으로 로그인하면 상품 위 `공급가정보`를 눌러 `공급업체정보` 팝업을 연다. 이 팝업의 공장·담당자·연락처·이메일·배송/인쇄 조건을 **원본 숫자 상품번호에 정확히 매칭**한다. 비슷한 상품명으로 공급처를 추정하거나 같은 업체의 담당자가 모든 상품에서 같다고 가정하지 않는다.
- 대표가 나중에 메일 발송을 지시하면 이 매칭을 조회하여 해당 상품의 수신처와 조건을 사용한다. 현재 지시는 정보 정리이며 메일 발송 지시가 아니다. 발송 시 상품번호·최신 공급처·수신 주소를 대조하고 기존 NAS 메일 API/함수를 우선한다. 누락된 주소는 다른 상품이나 검색 결과로 임의 보충하지 않는다.
- `_scraper/gift_supplier_registry.py`는 정확한 상품번호 조회, 입력 필드 검증, 부분 수집 시 기존 매칭 보존, 변경 이력, 오래된 자료의 덮어쓰기 방지, 로그의 연락처 제외를 구현한다. 동일 시각에 충돌하는 공급처가 있으면 해당 일괄 가져오기를 취소한다. 이 도구는 메일을 발송하거나 웹 관리자 인증을 제공하지 않는다.
- 데이터는 저장소 **밖**의 `../_private/nadaun-shop/gift/`에 둔다. 루트는 실행 파일 위치에서 계산하므로 Mac/Windows 절대경로를 코드에 넣지 않는다. `suppliers.sqlite3`와 원본 입력 JSON은 Git·Vercel·R2·공개 상품 JSON에 넣지 않는다. 웹하드 등 외부 서비스의 계정/비밀번호 필드는 수집하지 않는다. NAS 신규 비공개 디렉터리와 입력 파일의 그룹/기타 권한 0을 확인했다. 웹의 대표 전용 관리자 화면은 아직 없다.
- 대표가 제공한 두 캡처의 상품 **187684 / 2602809**를 해당 화면의 공급처와 매칭해 저장하고 재조회했다. 근거 종류는 `owner_screenshot`이며, 로그인 세션에서 자동 수집한 데이터로 표시하지 않는다. **현재 2개만 등록됐으며 전체 상품 매칭·자동 갱신·공장 공급단가 수집은 미완료**다. 실행 중인 Chrome에 직접 연결을 여러 번 요청했으나 런타임에서 `Browser is not available: chrome`을 반환했다. 새 브라우저로 바꾸지 말고 사용 중인 Chrome의 정상 연결을 복구한 뒤 공식 내보내기/API 제공 여부부터 확인한다.
- 팝업의 판매수수료는 제품 공급단가가 아니다. 대표가 요청한 약 20% 가산의 기준이 기존 판매단가인지 실제 공장 공급단가인지는 아직 답변 대기다. 기준 확정 전 공개 가격을 바꾸지 않는다. 수량별 단가·인쇄 옵션·배송비·부가세를 각각 보존하고, 결제에 필요한 정보가 미확정이면 임의 합계를 결제받지 않는다.
- 검증: 공급처 저장소 단위 검사 12건. 동일 이름의 서로 다른 상품, 이메일 혼합/헤더 삽입, 비공개 경로 우회, 자료 충돌 시 일괄 롤백, 중복 입력, 과거 자료, 부분 갱신, 메일 누락을 확인했다.

실행은 프로젝트 폴더에서 한다. `lookup` 결과는 새 비공개 JSON에만 기록하며 터미널에는 이메일을 출력하지 않는다.

```sh
python3 _scraper/gift_supplier_registry.py status
python3 _scraper/gift_supplier_registry.py import --input ../_private/nadaun-shop/gift/owner-screenshots-2026-09-08.json
python3 _scraper/gift_supplier_registry.py lookup --product 187684 --output ../_private/nadaun-shop/gift/lookups/187684-review.json
```

조회 파일이 이미 있으면 다른 파일명으로 보존한다. 실제 발송 전에는 반환 자료의 `evidence`, `age_days`, `email_present`를 확인하며, 조회 성공을 발송 완료로 보고하지 않는다.
