# 나다운 샵 주문·결제·관리자 구축 기준

2026-09-07 대표 요청. 현재 구현된 기능과 아래 설계를 혼동하지 않는다. 2026-09-08 주문 API·토스 결제창 호출·서버 승인·주문 조회·관리자 UI 코드를 추가했다. 실제 운영 DB와 PG 키는 미연결이며 주문 접수/실결제는 비활성이다. 최신 구현 범위와 개통 조건은 맨 아래 절을 따른다.

## 순서

디자인 수정과 함께 토스페이먼츠 계약·개발 연결을 준비한다. 구현은 주문 저장소·관리자 인증 → 테스트 결제 → 주문·출고 처리 → 취소·환불 검증 → 실결제 개통 순서다. 계약 신청 또는 라이브 키 발급을 완료했다고 가정하지 않는다.

## 구매 흐름

1. 서버가 상품번호·옵션·수량으로 가격과 재고, 배송비를 다시 계산한다. 공개 JSON이나 브라우저의 합계는 결제 기준 원장이 아니다. 2026-09-08 확정 기본 배송비는 주문당 4,500원, 중량 스탠드 포함 주문은 7,000원이며 혼합/복수 수량이어도 한 번 합산한다. `assets/shop/shipping.js`의 정책을 최신 상품 배송군으로 다시 계산한다. 공급처 가격만 있거나 추가 운임·납기 확인이 필요한 상품은 확정 견적 후 결제한다.
2. 주문번호, 상품·옵션·금액·배송 조건의 당시 스냅샷을 비공개 데이터베이스에 저장한다. 주문 정보는 Git·공개 카탈로그·브라우저 localStorage에 저장하지 않는다.
3. 토스 결제창을 연다. 인증 후 돌아온 주문번호·금액을 서버 기록과 비교하고 서버에서 승인을 요청한다.
4. 실제 결제 조회 결과로 결제 상태를 확정한다. 인증 성공 URL만으로 결제완료 처리하지 않는다. 승인 응답 유실·중복 클릭·중복 웹훅에서도 중복 주문과 중복 승인을 방지한다.
5. 고객에게 본인 주문서와 결제 상태를 보여주고 관리자에게 출고 대기 주문을 표시한다. 고객 주문 조회에도 본인 확인을 적용한다.

## 관리자 화면

- `/admin`: 승인된 직원 로그인, 역할별 권한과 변경 이력. 공개 UI의 링크 숨김은 인증이 아니다.
- 대시보드: 신규 결제, 출고 대기, 취소·반품 요청, 렌탈 픽업·반납 예정.
- 주문: 주문번호/고객 검색, 상품·옵션·배송지·결제 내역, 출고 처리·택배사·운송장. 결제 상태와 배송 상태는 별도로 관리한다.
- 취소·환불: 요청 접수 → 반품·환불 조건 및 가능 금액 확인 → PG 취소 결과 조회 → 내부 상태 확정. 전액·부분 취소, 재시도와 중복 요청을 검증한다.
- 렌탈: 장비·기간·픽업·반납 관리. 기존 NAS `nadaun_order_sync` 함수와 Google Calendar API를 우선하고 같은 예약을 두 번 만들지 않는다.
- 상품 연동: 출처별 마지막 성공 시각, 가격/내용/품절 변경, 수집 실패, 수동 수정값과 재고 우선순위. 공급처 목록 누락을 즉시 삭제로 처리하지 않는다.

## 기본 설정 — 대표가 제공한 아임웹 화면 참고

- 사이트 이름·80자 이내 사이트 설명·파비콘·공유 대표 이미지.
- 고객에게 발송되는 메일/SMS의 사이트 이름. 발송 공급자 연결 및 발송 동의/계약은 별도 검증한다.
- 공개/준비 상태, 관리자 미리보기, 구매 완료 후 주문서 이동.
- 모바일 확대를 허용한다. 없는 주소는 올바른 404를 제공하고 홈 이동 링크를 둔다. 모든 없는 주소를 200 홈으로 바꾸지 않는다.
- 통계에서 관리자 방문 제외. 로그인 유지는 인증 서비스의 세션·만료 정책으로 구현한다.
- 언어별 사이트가 준비되기 전 언어 자동 이동을 만들지 않는다. 아임웹 전용 중국 접속 옵션은 그대로 복제할 기능으로 보지 않는다.

## 상품 동기화 범위와 미완료 작업

- 자체 스마트스토어, KPP, LDL-MOUNT, 아임웹 DJI/프로모션은 기존 일일 수집에 연결되어 있다. 즉시 동기화는 아니다.
- **PLTHINK 유쾌한생각 전체 브랜드 상품**을 추가 대상으로 확정했다. `_scraper/makeshop.py`의 과거 수집 경험을 참고하되 최신 목록·옵션·품절 전수 대조 후 새 수집기에 편입한다. 아직 연결 완료가 아니다. 141개 브랜드 메뉴 확인 후 공개 사이트의 요청 제한이 발생해 전체 수집을 중단했다. 요청 직렬화·2.1초 이상 간격·차단 감지·대기 가드가 있는 새 읽기 수집기는 준비했으며 정상 접근 복구 또는 공식 상품 피드가 필요하다.
- 아임웹 폐쇄 전 자체 상품·옵션·정책과 이미지 바이너리를 NAS/R2에 보존한다. JSON에 외부 CDN 주소만 남기는 것은 보존 완료가 아니다.
- 기프트샵은 현재 분류/대표 상품 링크 연결이며 전체 재고·주문 통합은 아니다. 2026-09-08 대표가 전체 상품 미러링과 자체 결제, 약 20% 가산, 상품별 비공개 공급처 매칭을 추가 요청했다. 공급처 팝업 위치는 로그인 후 상품 위 `공급가정보`다. 캡처 2개 상품의 공급처/이메일만 저장소 밖의 비공개 DB에 매칭했다. 전체 자동 수집·웹 관리자·공급단가·가산 기준·PG는 아직 미완료다. 실제 메일은 추후 대표 지시에 따라 상품번호로 이 매칭을 조회하여 발송한다. 상세 실행법은 `RUNBOOK.md`의 기프트 절을 따른다.
- 공급처 즉시 반영은 공식 API/변경 통지 지원을 확인하고, 미지원 시 실제 수집 소요 시간과 허용 호출량에 맞춰 변경 확인 주기를 정한다. 수집 오류는 품절과 구분한다.

## 검증

2026-09-08 야간 구현: PLTHINK 전체 수집 재개·검증 후 자동 카탈로그 반영과 한정 배포 워커, 기프트 전체 목록 이후 공개 옵션/수량별 단가 수집 워커를 추가했다. 기프트 실결제·20% 가산 가격 발행·공장정보 전수 자동 매칭, 토스 PG와 주문 관리자 연결은 아직 완료가 아니다. 실행/발행 증거와 실제 완료 수는 `RUNBOOK.md`의 야간 수집 절에 지정한 상태 파일을 확인한다.

테스트 환경에서 결제 승인·실패·중복 요청·금액 변조·품절·배송비·주문 조회 권한·부분 환불·PG 응답 유실·복구를 확인한다. 실제 상점 키와 비공개 주문 저장소가 준비된 뒤 실결제를 활성화한다. 시크릿 키를 대화·프런트 코드·Git에 넣지 않는다.

토스 공식 근거: https://docs.tosspayments.com/guides/v2/get-started/payment-flow


## 2026-09-08 토스 결제 연결 코드와 실제 개통 상태

대표는 “아직 발급 전이야. 우선 결제창에 토스페이먼츠 연결해놔. 내일 전화할테니까”라고 확정했다. 계약·PG 키 발급은 완료로 취급하지 않는다. 운영 주문 API의 키를 임의의 공용 샘플 키로 대신하지 않는다. 아래 사전연동 테스트 절의 공개 문서용 위젯 클라이언트 키는 실제 주문·승인 API와 분리된 미리보기에만 사용한다.

### 구현 파일과 동작

- `api/orders.js` + `server/commerce/`: 서버에서 상품번호·옵션·수량을 검증하고 자체 판매가/파트너 정가 기준과 주문당 배송비 4,500원/중량 스탠드 7,000원을 재계산한다. 서버에 배포한 검증 카탈로그로 계산하며 브라우저 합계는 받지 않는다. 현재 공개 상품에는 실제 보유 수량 원장이 없으므로 **관리자가 수량·옵션별 재고·납기를 직접 확인하고 금액을 확정한 주문만 결제 허용**한다. 이 구현을 자동 재고 예약 또는 전 상품 즉시 결제라고 설명하지 않는다.
- `assets/shop/commerce.js`: 배송 정보 접수 → `/orders.html` → 확정 금액 동의 → Toss Payments v2 CARD 통합 결제창 → 서버 확인 → 주문 조회. `/admin.html`과 `/admin`은 관리자 로그인/주문 목록/재고 확인/금액 확정/결제 재조회/운송장 입력 UI다. 관리자 API는 모든 요청에 인증을 요구한다.
- 서버의 주문번호·금액·결제키를 고정한 후 토스 승인 API에 영속 멱등키를 사용한다. 승인 후 결제 조회 API의 주문번호/결제키/KRW/금액/상태를 다시 대조한다. 응답 유실은 같은 키로 확인하며 성공 URL만으로 결제완료 처리하지 않는다. 주문과 상태 이벤트는 조건부 버전 갱신 및 DB 배치로 함께 저장하도록 구현했다.
- 배송지와 연락처는 AES-256-GCM 암호문으로만 DB에 저장한다. 세션은 서명된 Secure/HttpOnly/SameSite 쿠키이며 고객 주문 소유권과 관리자 권한을 분리한다. 고객용 localStorage에는 상품번호·옵션·수량만 남긴다. 관리자 조회 응답은 인증 후에만 복호화하며 `no-store`다.
- 테스트/라이브 모드를 주문에 고정한다. 테스트 주문은 실제 출고를 서버에서 거절하고 화면에 테스트라고 표시한다. 환경이 바뀐 키로 이전 모드의 결제를 승인하지 않는다.
- 공개 접근 `/server/*`는 404이며 주문/관리 페이지는 noindex, 사이트맵 제외다. 결제키·배송지·공장/공급처 비공개 정보는 정적 HTML·공개 상품 JSON·R2로 내보내지 않는다.

### 실제 미연결 사항과 활성화 절차

2026-09-08 04:40 KST Vercel 프로젝트 환경변수 목록은 비어 있었다. NAS의 `TOSS_SHOPPING_*`는 토스쇼핑 판매자 연결이며 **토스페이먼츠 PG 키가 아니다**. R2 S3 키에는 D1 DB 관리 권한이 없다. 기존 NAS SSH도 키 인증에 실패했으므로 임의 DB 서버 연결/터널 변경은 하지 않았다.

`_scraper/commerce_setup.cjs`의 `init`을 실행해 저장소 밖 `_Site/_private/nadaun-shop/commerce/`에 `configuration.json`, `owner-login.private`를 생성했다. 폴더 700/파일 600으로 요청했고 기존 파일을 덮어쓰지 않는다. 설정값·관리자 비밀번호는 대화나 로그에 출력하지 않는다. 최종 NAS 접근 제어는 NAS 공유 ACL도 함께 확인한다.

1. 토스페이먼츠 **주문서형·결제창형 연동 키**의 테스트 `test_gck_`/`test_gsk_`, 계약 후 라이브 `live_gck_`/`live_gsk_` 쌍을 비공개 설정에 등록한다. `SHOP_PAYMENT_MODE=test|live`로 명시한다.
2. 실제 비공개 Cloudflare D1 DB를 생성하고 D1 쓰기 범위 API 토큰/계정 ID/DB ID를 등록한다. `node _scraper/commerce_setup.cjs schema` → `check`로 테이블을 다시 조회한다. 현재 어댑터는 공식 REST 배치 규격에 맞춘 구현이며 **실제 D1의 NULL/숫자 바인딩, 배치 원자성 및 장애 복구는 연결 후 별도 종단 검증이 필요**하다. 로컬 SQLite 테스트만으로 D1 검증 완료라 하지 않는다.
3. `SHOP_ORDER_DATA_KEY`, `SHOP_SESSION_KEY`, `SHOP_ADMIN_PASSWORD_HASH`는 위 private 설정에 생성되어 있다. `SHOP_CF_ACCOUNT_ID`, `SHOP_D1_DATABASE_ID`, `SHOP_D1_API_TOKEN`, `SHOP_TOSS_CLIENT_KEY`, `SHOP_TOSS_SECRET_KEY`와 함께 Vercel 서버 환경변수로 등록한다. 암호화 키를 바꾸면 기존 배송지 복호화가 불가능하므로 백업/복구 절차를 먼저 확정한다.
4. 실제 수탁자·배송사·국외 이전 및 보존/파기 절차를 개인정보처리방침에 반영하고 운영 주문 접수를 열기 전에 확인한다. 현재 라이브 방침은 직접 수집 비활성 상태에 맞춰 유지했다.
5. 별도 테스트 DB/테스트 키에서 승인·실패·금액 변조·응답 유실·중복 요청·모바일 결제 리다이렉트·관리자 로그인/출고 차단을 종단 검증한다. 테스트 주문은 라이브 DB로 옮기지 않는다.
6. `SHOP_ORDERS_ENABLED=true`는 검증이 끝난 환경에서만 설정하고 재배포 후 API/화면을 재조회한다. 값이 없거나 DB/키/인증 설정이 잘못되면 현재 상담 화면을 유지하며 개인정보 입력 폼과 결제창을 열지 않는다.

### 남은 구현

운영 DB 프로비저닝, 실제 PG 종단 테스트와 실결제 활성화, 고객의 다른 기기 주문 조회 인증, 자체 재고 수량/예약 원장, 취소 요청·전액/부분 환불 API, 자동 결제 웹훅 재검증/재조정, 직원별 역할/계정, 주문 검색·페이지 이동, 이메일/SMS 알림, 주문 보존/파기 자동화와 DB 백업/복구 검증은 아직 완료가 아니다. 현재 관리자 최근 100건/고객 최근 50건 조회다. 결제 결과가 장기 불명확하거나 만료된 진행 주문은 관리자 결제 재조회/상담으로 해결해야 한다. 사이트 화면이 있다는 이유로 전체 주문 운영 시스템 완성이라고 보고하지 않는다.

### 검증 근거

`_scraper/tests/commerce.test.cjs`: 실제 메모리 SQLite로 가격·옵션·배송비, 주문 멱등성, 주소 암호화, 고객 소유권, 관리자 승인 CAS/이벤트 롤백, 결제 값 대조/응답 유실 복구, 테스트 출고 차단, 세션/Origin/속도 제한을 검증한다. 토스와 D1의 외부 통신은 대역으로 검증하며 실결제는 발생하지 않는다.

`_scraper/tests/commerce-ui.test.cjs`: 서버 확정 금액으로만 결제창 호출, 금액 동의 전 차단, 서비스 비활성 시 주소 입력/SDK 로드 차단, 상품/고객 문자열 HTML 이스케이프와 테스트 출고 버튼 숨김을 검증한다. Chrome 연결이 없어 실제 브라우저/모바일 시각 검수는 아직 하지 못했다.

배포 증거: `_scraper/.sync-state/commerce-release.json`, `.sync-state/commerce-live.json`. 운영 환경의 `/api/orders?action=config`가 `ordersEnabled:false`이면 결제 개통이 완료된 것이 아니다.


## 2026-09-08 계약 상담 전 주문서형 테스트 연결

대표는 토스에 전화하여 신청을 시작할 수 있도록 먼저 결제 UI를 연결하라고 명확히 했다. 이어 SDK v2 결제창형/주문서형 문서와 공개 `test_gck_docs_` 클라이언트 키를 제공했다. **실결제 미발급을 이유로 사전 테스트 UI까지 막지 않는다.**

- 현재 선택은 **주문서형**: `widgets({customerKey: ANONYMOUS})` → `setAmount()` → `renderPaymentMethods(DEFAULT)` + `renderAgreement(AGREEMENT)` → `requestPayment()`다. 샘플의 고정 고객 정보와 쿠폰은 적용하지 않는다.
- `/checkout.html`에 금액이 확인된 구매 상품이 있으면 상품·옵션·수량·배송비 합계로 테스트 위젯을 표시한다. 불명확한 금액은 위젯 호출을 막는다. 실주문 API가 준비되면 이 미리보기 대신 기존 배송 정보 접수가 표시되며, 재고·납기 확정 후 `/orders.html`에서 서버 확정 금액의 운영 위젯을 사용한다.
- `/payment-test.html`은 장바구니 없이 1,000원으로 결제수단/약관/인증 화면을 확인하는 페이지다. noindex·사이트맵 제외. 테스트임을 확인하는 체크박스와 실제 청구·주문·배송이 없다는 안내를 표시한다.
- **미리보기는 승인 API를 호출하지 않는다.** 인증 반환 URL은 이 테스트 페이지로만 돌아오며, 쿼리 정보를 주소창에서 즉시 제거하고 실제 주문/결제완료로 처리하지 않는다. 운영 승인·저장·환불 종단 검증이 끝났다는 증거로 사용하지 않는다.
- 공개 문서용 클라이언트 키는 미리보기 코드에만 들어간다. 서버 PG 어댑터는 동일 모드의 `gck/gsk` 쌍만 허용하며 예전 `ck/sk` 키로 이 위젯을 초기화하지 않는다. 기존 승인 금액 대조·멱등성·권한 검증은 유지한다.
- 키 발급·상점 신청·카드사 심사·실거래 활성화는 아직 완료가 아니다. Chrome 연결이 없어 실제 토스 UI·모바일 리다이렉트 육안 검증은 미확인이다. 자동 테스트/배포 검증과 구분한다.
- 공식 근거: https://docs.tosspayments.com/sdk/v2/js/payment-window , https://docs.tosspayments.com/guides/v2/payment-widget/integration . 상담용 요약은 `TOSS-SETUP.md`다.
